
enum Color { red, blue, green }
enum Direction { Up = 1, Down, Left, Right }
let c: Color = Color.red; // we can use the enum directly
let d: Direction = Direction.Up; // we can also use the enum with its values